# create-pcboo

Create a PCBoo project with `bun create pcboo my-board`.

The generated repository uses normal TypeScript files, Bun tests, a deterministic PCBoo lockfile, and no telemetry. PCBoo remains experimental; manufacturing output must pass its explicit verification gates before production use.
